name: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Setup .NET Core SDK
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '4.5'  

    - name: Restore dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build --configuration Release

    - name: Run tests
      run: dotnet test --configuration Release

    - name: Build and push Docker image 
      run: | 
        docker build -t ${{ secrets.ACR_LOGIN_SERVER }}/tech-hub-server:latest .
        docker login ${{ secrets.ACR_LOGIN_SERVER }} -u ${{ techhub6 }} -p ${{ Kp6aigD4VURyJxcjBJQ9APs2zjiVeMm8/HG1Ac1t0V+ACRDadIhl }}
        docker push ${{ secrets.ACR_LOGIN_SERVER }}/tech-hub-server:latest
